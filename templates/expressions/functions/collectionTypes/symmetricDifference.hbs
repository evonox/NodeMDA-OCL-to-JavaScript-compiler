((
    (({{>expression firstArgument}}).filter( (item) => {
        return ( ({{>expression args.[0]}}).indexOf(item) &lt; 0 )
    }))

).concat(
    (({{>expression args.[0]}}).filter( (item) => {
        return ( ({{>expression firstArgument}}).indexOf(item) &lt; 0 )
    }))
))